# Seed DNS records — generated from .env by scripts/render-config.sh.
#
# Format: `<IP> <hostname>` per line, hosts(5) syntax. Edit at runtime to
# add records discovered from the CoreDNS query log (capture-then-respond).
# CoreDNS reloads on file change.

${NTP_RECORD_LINE}
